exec vai --list
cmp stderr stderr.txt

exec rm vai.yaml
exec touch vai.yaml
! exec vai --list
stderr 'ERRO no tasks available'

-- vai.yaml --
default:
  - cmd: echo "default"

hello-world:
  - cmd: echo "Hello, World!"

another-task:
  - cmd: echo "Another task"
-- stderr.txt --
Available:

- default
- another-task
- hello-world
