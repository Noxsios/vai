! exec maru2 -l dne
stderr 'ERRO invalid level: "dne"'

exec maru2 --log-level debug
cmp stderr stderr.txt
cmp stdout stdout.txt

-- stderr.txt --
$ echo "default"
-- stdout.txt --
default
-- tasks.yaml --
default:
  - run: echo "default"
